# PostgreSQL

Run a new container:

```sh
make start
```

Connect to PostgreSQL console:

```sh
make psql
```

Stop (delete) the container:

```sh
make stop
```

Clean storage (from volume):

```sh
make clear
```
